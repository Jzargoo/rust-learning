[package]
name = "tiny-kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
volatile = "0.2.6"



[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[features]
default = ["boot-limine"]
boot-limine=[]